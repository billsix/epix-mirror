/* -*-ePiX-*- */
#include "epix.h"
using namespace ePiX;

int main() 
{
  bounding_box(P(-1,-1),P(1,1));
  unitlength("0.5pc");
  picture(1,1);
  //  offset(0,-1);

  begin();
  box(P(0,0));

  end();
}
