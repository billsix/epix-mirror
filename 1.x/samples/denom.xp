/* -*-ePiX-*- */
/* denom.c -- March 28, 2002 */
#include "epix.h"
using namespace ePiX;

const int N = 30; // maximum denominator plotted

main() 
{
  unitlength("0.01in");
  bounding_box(P(-2,0), P(2,1));
  picture(P(400,100));

  begin();   

  h_axis(2*x_size);
  v_axis(2);

  h_axis_labels(x_size, P(-12, -12));

  dot_size(1);
  for (int i=1; i< N; ++i) 
    for (int j=i*x_min; j <= i*x_max; ++j) 
      if (gcd(i, j) == 1) 
        dot(P(j*1.0/i, 1.0/i));

  end();
}

